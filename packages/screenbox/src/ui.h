// Hand-rolled wheel view: HSV disc with draggable light pins, global brightness
// bar, light pills. Mirrors packages/client ColorWheel + App wheel view.
#pragma once

namespace ui {
void begin();
void loop();
}
