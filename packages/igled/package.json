{
  "name": "@lightbox/igled",
  "version": "0.1.0",
  "private": true,
  "description": "From-scratch ESP32 firmware for the Govee curtain matrices (replaces WLED): buffered DDP listener + native routines",
  "scripts": {
    "compile": "pio run -e couch1 -e window",
    "flash": "pio run -e igled -t upload",
    "ota:couch1": "pio run -e couch1_ota -t upload",
    "ota:window": "pio run -e window_ota -t upload",
    "monitor": "pio device monitor -b 115200"
  }
}
